#include<stdio.h>
#include<stdlib.h>
/*
int main () {
	
	int secretNumber = 6;
	int guess;
	int lives=3;
	
	printf("guess a number between 1 and 10 (you got %d lives)\n", lives);
	
	while(guess!=secretNumber && lives != 0) {
	scanf("%d", &guess);
	lives--;
	if (guess==secretNumber) printf("You Won!");
	else if (lives == 0) printf("you ran out of lives");
	else if (lives == 1) printf("You lost, try again :/ (you have %d life left)\n", lives);
	else printf("You lost, try again :/ (you have %d lives left)\n", lives);
	
	}

	return 0; 
}*/
